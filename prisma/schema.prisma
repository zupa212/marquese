generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Click {
  id        Int      @id @default(autoincrement())
  category  String
  label     String
  page      String
  createdAt DateTime @default(now())
}
